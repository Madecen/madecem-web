---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';

// Importación de assets
import heroIA from '../assets/hero-ia.webp';
import heroVision from '../assets/hero-vision.webp';
import visionindustrialImg from '../assets/visionindustrial.webp'; 
import logoMadecem from '../assets/logo-madecem.svg';
---

<Layout title="MADECEM | Agentes de IA e Industria 4.0">
    <main class="relative isolate bg-[#020617] text-white">
        
        <div class="absolute inset-x-0 -top-40 -z-10 transform-gpu overflow-hidden blur-3xl sm:-top-60" aria-hidden="true">
            <div class="relative left-[calc(50%-11rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 rotate-[30deg] bg-gradient-to-tr from-[#0ea5e9] to-[#2563eb] opacity-20 sm:left-[calc(50%-30rem)] sm:w-full"></div>
        </div>

        <section class="relative mx-auto max-w-7xl px-6 pb-24 pt-10 sm:pb-32 lg:px-8 lg:py-24 overflow-hidden">
            <div id="hero-carousel" class="relative">
                <div class="carousel-slide flex flex-col lg:flex-row items-center gap-12 transition-all duration-1000">
                    <div class="lg:w-1/2 lg:flex-shrink-0">
                        <span class="rounded-full bg-blue-500/10 px-3 py-1 text-sm font-semibold leading-6 text-blue-400 ring-1 ring-inset ring-blue-500/20">
                            Eficiencia Autónoma
                        </span>
                        <h1 class="mt-8 text-4xl font-bold tracking-tight text-white sm:text-6xl">
                            IA que <span class="text-blue-500 text-glow">elimina</span> tus costes de inactividad.
                        </h1>
                        <p class="mt-6 text-lg leading-8 text-gray-300">
                            No esperes a que tus operarios detecten el cuello de botella. Nuestros agentes autónomos toman decisiones en milisegundos para optimizar tu cadena de suministro.
                        </p>
                        <div class="mt-10 flex items-center gap-x-6">
                            <a href="#contacto" class="rounded-md bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-500/30 hover:bg-blue-500 hover:scale-105 transition-all">
                                 Desplegar mi Agente IA
                            </a>
                        </div>
                    </div>
                    <div class="lg:w-1/2 flex justify-center relative">
                        <Image src={heroIA} alt="IA Industrial MADECEM" width={450} height={450} class="w-full max-w-[450px] rounded-2xl shadow-2xl ring-1 ring-white/10 object-cover" />
                    </div>
                </div>

                <div class="carousel-slide hidden flex flex-col lg:flex-row items-center gap-12 transition-all duration-1000">
                    <div class="lg:w-1/2 lg:flex-shrink-0">
                        <h2 class="mt-8 text-4xl font-bold tracking-tight text-white sm:text-6xl">
                            Si tu ojo falla, <span class="text-emerald-500 text-glow">nuestra IA no</span>.
                        </h2>
                        <p class="mt-6 text-lg leading-8 text-gray-300">
                            Control de calidad ultra-preciso mediante Computer Vision. Detectamos defectos en tiempo real que son invisibles para el ojo humano.
                        </p>
                    </div>
                    <div class="lg:w-1/2 flex justify-center relative">
                        <Image src={heroVision} alt="Visión Artificial" width={450} height={450} class="w-full max-w-[450px] rounded-2xl shadow-2xl object-cover" />
                    </div>
                </div>

                <div class="mt-12 flex justify-center gap-3">
                    <button class="dot h-1.5 w-8 rounded-full bg-blue-600 transition-all"></button>
                    <button class="dot h-1.5 w-8 rounded-full bg-white/20 transition-all"></button>
                </div>
            </div>
        </section>

        <section id="servicios" class="py-24 bg-slate-900/40">
            <div class="mx-auto max-w-7xl px-6 lg:px-8">
                <div class="mx-auto max-w-2xl text-center mb-16">
                    <h2 class="text-blue-500 font-semibold tracking-wide uppercase">Capacidades</h2>
                    <p class="mt-2 text-3xl font-bold sm:text-4xl">Soluciones de Agentes IA</p>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div class="p-8 rounded-3xl bg-white/5 border border-white/10 hover:border-blue-500 transition-all">
                        <div class="text-blue-400 mb-6 text-3xl">🤖</div>
                        <h3 class="text-xl font-bold mb-3">Agentes Operativos</h3>
                        <p class="text-gray-400">Automatización de decisiones en ERP y MES para eliminar errores humanos en la cadena de mando.</p>
                    </div>
                    <div class="p-8 rounded-3xl bg-white/5 border border-white/10 hover:border-emerald-500 transition-all">
                        <div class="text-emerald-400 mb-6 text-3xl">👁️</div>
                        <h3 class="text-xl font-bold mb-3">Agentes de Visión</h3>
                        <p class="text-gray-400">Monitoreo constante de líneas de producción con alertas instantáneas ante anomalías de calidad.</p>
                    </div>
                    <div class="p-8 rounded-3xl bg-white/5 border border-white/10 hover:border-purple-500 transition-all">
                        <div class="text-purple-400 mb-6 text-3xl">📉</div>
                        <h3 class="text-xl font-bold mb-3">Análisis Predictivo</h3>
                        <p class="text-gray-400">Predecimos fallos antes de que ocurran, reduciendo el mantenimiento reactivo en un 35%.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="py-24 bg-[#020617]">
            <div class="mx-auto max-w-7xl px-6 lg:px-8">
                <div class="text-center mb-20">
                    <h2 class="text-blue-500 font-bold uppercase tracking-widest">Metodología</h2>
                    <p class="mt-2 text-3xl font-bold sm:text-5xl text-white">De los datos a la acción</p>
                </div>
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
                    <div class="group relative p-8 rounded-3xl bg-gradient-to-b from-white/10 to-transparent border border-white/5 hover:border-blue-500/50 transition-all">
                        <span class="block text-6xl font-black text-blue-600/40 mb-4 group-hover:text-blue-500 transition-colors">01</span>
                        <h4 class="text-2xl font-bold text-white mb-3">Auditoría</h4>
                        <p class="text-gray-400 leading-relaxed">Analizamos tus flujos de datos y detectamos los cuellos de botella críticos.</p>
                    </div>

                    <div class="group relative p-8 rounded-3xl bg-gradient-to-b from-white/10 to-transparent border border-white/5 hover:border-blue-500/50 transition-all lg:mt-8">
                        <span class="block text-6xl font-black text-blue-600/40 mb-4 group-hover:text-blue-500 transition-colors">02</span>
                        <h4 class="text-2xl font-bold text-white mb-3">Conexión</h4>
                        <p class="text-gray-400 leading-relaxed">Integramos nuestros agentes IA con tus sistemas PLC, ERP o MES actuales.</p>
                    </div>

                    <div class="group relative p-8 rounded-3xl bg-gradient-to-b from-white/10 to-transparent border border-white/5 hover:border-blue-500/50 transition-all">
                        <span class="block text-6xl font-black text-blue-600/40 mb-4 group-hover:text-blue-500 transition-colors">03</span>
                        <h4 class="text-2xl font-bold text-white mb-3">Entreno</h4>
                        <p class="text-gray-400 leading-relaxed">Modelamos la IA basándonos en tu histórico de planta para máxima precisión.</p>
                    </div>

                    <div class="group relative p-8 rounded-3xl bg-gradient-to-b from-white/10 to-transparent border border-white/5 hover:border-blue-500/50 transition-all lg:mt-8">
                        <span class="block text-6xl font-black text-blue-600/40 mb-4 group-hover:text-blue-500 transition-colors">04</span>
                        <h4 class="text-2xl font-bold text-white mb-3">Acción</h4>
                        <p class="text-gray-400 leading-relaxed">Despliegue de agentes autónomos que optimizan tu producción 24/7.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="py-24 bg-slate-900/30">
            <div class="mx-auto max-w-3xl px-6 lg:px-8">
                <h2 class="text-3xl font-bold text-center mb-12">Preguntas Frecuentes</h2>
                <div class="space-y-6">
                    <details class="group rounded-2xl bg-white/5 border border-white/10 p-6 transition-all">
                        <summary class="list-none flex justify-between items-center cursor-pointer font-bold">
                            ¿Es compatible con mi maquinaria antigua?
                            <span class="text-blue-500 group-open:rotate-45 transition-transform">+</span>
                        </summary>
                        <p class="mt-4 text-gray-400 leading-7">Sí. Utilizamos protocolos industriales estándar (OPC-UA, MQTT) para conectar sistemas antiguos con IA moderna.</p>
                    </details>
                    <details class="group rounded-2xl bg-white/5 border border-white/10 p-6 transition-all">
                        <summary class="list-none flex justify-between items-center cursor-pointer font-bold">
                            ¿Dónde residen los datos?
                            <span class="text-blue-500 group-open:rotate-45 transition-transform">+</span>
                        </summary>
                        <p class="mt-4 text-gray-400 leading-7">Ofrecemos opciones en la nube o local (Edge Computing) para garantizar la máxima seguridad y privacidad de tu planta.</p>
                    </details>
                </div>
            </div>
        </section>

        <footer class="bg-[#010409] border-t border-white/5 pt-16 pb-8">
            <div class="mx-auto max-w-7xl px-6 lg:px-8 flex flex-col md:flex-row justify-between items-center gap-8">
                <div class="flex items-center gap-3">
                    <Image src={logoMadecem} alt="Logo MADECEM" width={48} height={48} />
                    <span class="text-2xl font-bold tracking-tighter uppercase">Madecem</span>
                </div>
                <div class="text-xs text-gray-500">
                    © 2026 MADECEM S.L. Inteligencia Aplicada a la Industria.
                </div>
            </div>
        </footer>

    </main>
</Layout>

<style>
    .text-glow { text-shadow: 0 0 20px rgba(59, 130, 246, 0.5); }
    .carousel-slide { animation: fadeIn 0.8s ease-out; min-height: 450px; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    .dot { cursor: pointer; }
</style>

<script>
    const slides = document.querySelectorAll('.carousel-slide');
    const dots = document.querySelectorAll('.dot');
    let currentSlide = 0;

    function showSlide(index: number) {
        slides.forEach((slide, i) => {
            slide.classList.toggle('hidden', i !== index);
            if(dots[i]) {
                dots[i].classList.toggle('bg-blue-600', i === index);
                dots[i].classList.toggle('bg-white/20', i !== index);
                (dots[i] as HTMLElement).style.width = i === index ? '2rem' : '0.5rem';
            }
        });
    }

    function nextSlide() {
        if (slides.length === 0) return;
        currentSlide = (currentSlide + 1) % slides.length;
        showSlide(currentSlide);
    }

    setInterval(nextSlide, 8000);

    dots.forEach((dot, i) => {
        dot.addEventListener('click', () => {
            currentSlide = i;
            showSlide(currentSlide);
        });
    });
</script>